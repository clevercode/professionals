= semantic_form_for @answer do |f|
  = f.inputs do
    .two-column
      = f.input :question
      = f.input :category, include_blank: false
    = f.input :content, label: 'Answer', input_html: { rows: 7 }

  = f.actions do
    = f.action :submit

